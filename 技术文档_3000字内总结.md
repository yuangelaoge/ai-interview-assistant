# 《迹学-全过程分析的 AIGC 写作合规检测系统》技术文档总结

## 一、项目背景

随着生成式人工智能进入高校写作教学，学生使用 AI 辅助写作越来越普遍，但传统 AIGC 检测工具主要关注最终文本，通常只给出黑盒分值或疑似率，难以区分“合理辅助”和“直接代写”，也缺少写作过程证据，容易造成误判并增加教师复核压力。

“迹学”面向高校文科与通识课程写作场景，提出“过程化合规写作”理念。系统不只检测终稿，而是记录学生从任务查看、资料检索、AI 辅助、正文编辑、版本变化、复制粘贴、声明反思到终稿提交的全过程，形成可追溯、可解释、可复核的证据链，帮助教师判断写作行为是否合规。

## 二、角色与功能

系统面向管理员、教师、学生三类用户。管理员负责教师、学生、课程和成员关系管理。教师负责发布任务、配置 AI 使用规则、维护课程知识库、查看提交过程、复核风险片段、排查相似度并完成最终审阅。学生负责查看任务、在线写作、自动保存、版本管理、AI 辅助、资料检索、来源标记、AI 使用声明、写作反思、终稿提交和验证追问回答。

学生端以 Tiptap 富文本写作工作台为核心，系统会记录键入批次、复制粘贴、AI 插入、资料检索插入、版本快照和来源链接。AI 辅助限定在提纲、结构、润色、改写、摘要等受控场景，调用过程会写入日志。教师端不只展示风险分数，还展示正文、风险片段、规则命中原因、来源覆盖、AI 声明、写作反思、验证回答、时间线和相似度摘要，使教师基于证据完成通过、驳回或退修。

## 三、系统架构

系统采用前后端分离架构，整体分为用户层、应用层、引擎层和数据层。用户层提供学生端、教师端、管理端入口；应用层包括任务管理、写作工作台、审阅中心、风险看板和分析看板；引擎层包括 AI 网关、AIGC 与过程规范引擎、相似性分析引擎；数据层使用 PostgreSQL、Redis、快照存储和知识库文件，支撑业务数据、缓存、版本证据和教学资料管理。

前端通过 `/api/v1` REST 接口访问后端，并使用 JWT 完成身份认证。普通请求由 Axios 封装，AI 流式生成使用 `fetch + ReadableStream` 接收 SSE。学生提交作业后，后端先校验字数、声明、反思和状态，再保存文档版本，并异步触发分析。分析流程汇总正文、版本、编辑事件、AI 调用、检索、复制粘贴、来源链接和声明信息，生成风险线索并刷新同任务相似度结果。

## 四、数据库与证据链

系统采用 PostgreSQL 作为核心数据库，并使用 Flyway 管理迁移。数据库以 `submission` 为中心组织证据链，

数据库主体遵循关系型范式，避免用户、课程、任务和提交信息重复存储；少量 JSON 字段用于保存规则配置、证据快照和弹性答案结构。通过这种设计，教师审阅时看到的不再是孤立终稿，而是正文内容、历史版本、编辑事件、AI 调用、检索记录、来源链接、声明反思、风险片段和相似度结果组成的完整证据链。

## 五、核心算法

系统的 AIGC 与过程规范引擎综合正文、版本、键入事件、提交时序、AI 调用、资料检索、复制粘贴、来源标注和学生声明等多源证据，生成可解释的风险候选。核心包含六类规则算子：R1 短时大段增长，识别异常集中输入；R2 截止前集中完成，识别临近截止的突击写作；R3 相邻版本异常跃迁，识别大幅替换或改写；R4 大段无来源内容，识别缺少来源覆盖的文本；R5 声明与记录不一致，识别 AI 使用声明矛盾；R6 段落风格突变，识别写作风格异常变化。

系统不会简单累加风险，而是进行句子定位、片段聚合、覆盖加权、补偿降权和全篇聚合，使局部风险不过度放大，同时让多处、大范围异常自然提高总风险。对疑似片段，系统还可生成验证追问，让学生解释写作思路或资料依据。最终结论由教师根据证据链复核决定，系统只提供辅助线索。

## 六、测试与反馈

最终版本完成了环境、性能、安全、业务可用性、部署和扩展性测试。前端 lint、生产构建和安全审计通过，`npm audit` 显示 0 漏洞；后端 Maven 单元测试共 32 项全部通过；页面与关键 API 响应正常；未登录访问受保护接口返回 401，错误角色登录和旧 refresh token 均能被拒绝。管理员建课、教师发布任务、学生写作提交、教师查看风险与复核等核心链路均验证通过。

用户测试中，学生对本项目总体评分平均为 7.88，高于对传统 AIGC 检测工具的评分。学生认可系统对合理写作过程的保护，并提出手动保存、UI 美化、复制标记等建议，项目已进行修复。教师反馈认为项目方案具有创新性和实用价值，并建议增加教师评语功能，系统已完成支持。

## 七、安装与总结

系统支持本地快速启动。评审人员在满足 README 环境要求后，可双击项目目录中的 `start.bat` 启动服务，默认前端地址为 `http://localhost:5173`。推荐流程为：管理员创建用户和课程，教师创建任务并上传知识库，学生完成写作、AI 辅助、资料检索、声明反思和提交，系统自动分析风险与相似度，教师查看证据并完成评阅。

迹学的核心价值在于将 AI 写作治理从“事后文本检测”推进到“全过程证据复核”。它围绕写作过程、资料来源、AI 使用、版本演化、风险线索和教师复核建立完整闭环。后续可继续优化算法自适应、教师端可视化、来源覆盖分析、相似片段聚类和高并发部署，并扩展到读书报告、课程论文、竞赛训练、培训写作和科研写作支持等场景。
