{
  "id": "kenshin-ryutsuisen",
  "name": "龙槌闪",
  "description": "突进到5格内1个敌人的相邻地格，并对其造成等同攻击力的物理伤害。",
  "keywords": [],
  "targetText": "5格内1个敌人",
  "effectTags": ["位移", "物理伤害"],
  "icon": "⬆️",
  "kind": "active",
  "type": "normal",
  "cooldownTurns": 3,
  "maxCharges": 0,
  "powerMultiplier": 1,
  "actionPointCost": 1,
  "code": "function executeSkill(context) { var caster = context.piece; var battle = context.battle; var target = selectTarget({ type: 'piece', range: 5, filter: 'enemy' }); if (!target || target.needsTargetSelection) return target; var targetPiece = battle.pieces.find(function(p) { return p.instanceId === target.instanceId && p.currentHp > 0; }); if (!targetPiece) return { success: false, message: '目标不存在' }; var dirs = [{x:0,y:-1},{x:1,y:0},{x:0,y:1},{x:-1,y:0}]; for (var d = dirs.length - 1; d > 0; d--) { var j = Math.floor(Math.random() * (d + 1)); var tmp = dirs[d]; dirs[d] = dirs[j]; dirs[j] = tmp; } var landCell = null; for (var i = 0; i < dirs.length; i++) { var nx = targetPiece.x + dirs[i].x; var ny = targetPiece.y + dirs[i].y; var tile = battle.map.tiles.find(function(t) { return t.x === nx && t.y === ny; }); if (!tile || !tile.props.walkable) continue; var blocked = battle.pieces.some(function(p) { return p.x === nx && p.y === ny && p.currentHp > 0 && p.instanceId !== caster.instanceId; }); if (!blocked) { landCell = { x: nx, y: ny }; break; } } if (!landCell) return { success: false, message: '目标周围没有可落脚的位置' }; caster.x = landCell.x; caster.y = landCell.y; var dmgResult = dealDamage(caster, targetPiece, Math.floor(caster.attack * 1), 'physical', battle, 'kenshin-ryutsuisen'); var dmg = dmgResult.totalDamage !== undefined ? dmgResult.totalDamage : (dmgResult.damage || 0); return { success: true, message: caster.name + '发动龙槌闪，突进至' + targetPiece.name + '身边，造成' + dmg + '点伤害' }; }",
  "previewCode": "function calculatePreview(piece, skillDef) { var dmg = Math.max(1, Math.floor(piece.attack * skillDef.powerMultiplier)); return { description: '突进到5格内1个敌人的相邻地格，并对其造成' + dmg + '点物理伤害。', expectedValues: { damage: dmg } }; }"
}
