{
  "id": "shishio-infinite-blade-recast",
  "name": "无限刃·再发",
  "description": "失去1点生命，再次对2格内1个敌人发动无限刃。",
  "keywords": ["真实伤害"],
  "targetText": "2格内1个敌人",
  "effectTags": ["damage", "recast"],
  "icon": "⚔️",
  "kind": "active",
  "type": "normal",
  "cooldownTurns": 0,
  "actionPointCost": 0,
  "requiresTarget": true,
  "maxCharges": 0,
  "powerMultiplier": 1,
  "code": "function executeSkill(context) { var source = context.piece; var battle = context.battle; var killTag = source.statusTags && source.statusTags.find(function(t) { return t.type === 'shishio-kills'; }); var kills = killTag ? (killTag.intensity || 0) : 0; var mult = kills >= 2 ? 2.0 : kills >= 1 ? 1.5 : 1.0; var target = selectTarget({ type: 'piece', range: 2, filter: 'enemy' }); if (!target || target.needsTargetSelection) return target; var dist = Math.abs(target.x - source.x) + Math.abs(target.y - source.y); if (dist > 2) return { success: false, message: '目标超出无限刃范围（2格内）' }; if (target.currentHp <= 0) return { success: false, message: '目标已出局' }; dealDamage(source, source, 1, 'true'); if (source.currentHp <= 0) { removeSkillById(source.instanceId, 'shishio-infinite-blade-recast'); return { success: true, message: source.name + '支付生命后倒下，无法再次发动无限刃' }; } var dmg = Math.floor(source.attack * mult); dealDamage(source, target, dmg, 'true'); var msgs = [source.name + ' 失去1点生命再次发动无限刃，对' + target.name + ' 造成 ' + dmg + ' 点真实伤害']; removeSkillById(source.instanceId, 'shishio-infinite-blade-recast'); return { success: true, message: msgs.join('；') }; }",
  "previewCode": "function calculatePreview(piece, skillDef) { var killTag = piece.statusTags && piece.statusTags.find(function(t) { return t.type === 'shishio-kills'; }); var kills = killTag ? (killTag.intensity || 0) : 0; var mult = kills >= 2 ? 2.0 : kills >= 1 ? 1.5 : 1.0; var dmg = Math.floor((piece.attack || 0) * mult); return { description: '失去1点生命，再次对2格内敌人造成' + dmg + '点真实伤害。', expectedValues: { damage: dmg } }; }"
}
