{
  "id": "blackwidow-lethal-toxin",
  "name": "致命毒素",
  "description": "在当前地格放置1个毒素，并移动到3格内1个空地格。敌人触发毒素时会受到4点伤害。",
  "keywords": [],
  "targetText": "3格内1个空地格",
  "effectTags": ["陷阱", "位移"],
  "icon": "☽️",
  "kind": "active",
  "type": "normal",
  "cooldownTurns": 2,
  "maxCharges": 0,
  "powerMultiplier": 1,
  "actionPointCost": 2,
  "code": "function executeSkill(context) { var caster = context.piece; var battle = context.battle; var targetPos = selectTarget({ type: 'grid', range: 3, filter: 'all' }); if (!targetPos || targetPos.needsTargetSelection) return targetPos; var newX = targetPos.x; var newY = targetPos.y; var tile = battle.map.tiles.find(function(t) { return t.x === newX && t.y === newY; }); if (!tile || !tile.props.walkable) return { success: false, message: '目标位置不可行走' }; var occupied = battle.pieces.some(function(p) { return p.x === newX && p.y === newY && p.currentHp > 0; }); if (occupied) return { success: false, message: '目标位置已被占用' }; var toxinX = caster.x; var toxinY = caster.y; addPlayerStatusEffectById(caster.ownerPlayerId, { id: 'lethal-toxin-' + Date.now(), type: 'lethal-toxin', name: '致命毒素', currentDuration: -1, intensity: 4, value: toxinX, extraValue: toxinY }); addPlayerRuleById(caster.ownerPlayerId, 'rule-blackwidow-toxin-player'); caster.x = newX; caster.y = newY; return { success: true, message: caster.name + '在(' + toxinX + ',' + toxinY + ')放置了致命毒素，并移动到(' + newX + ',' + newY + ')' }; }",
  "previewCode": "function calculatePreview(piece, skillDef) { return { description: '在当前地格放置1个毒素，并移动到3格内1个空地格。敌人触发毒素时会受到4点伤害。', expectedValues: { damage: 4 } }; }"
}
