{"id":"earthshatter","name":"毁天灭地","description":"选择1个空地格落下，对以该格为中心的3x3区域内敌人造成等同攻击力的物理伤害。","keywords":[],"targetText":"1个空地格","effectTags":["位移","范围伤害"],"icon":"🌪","kind":"active","type":"super","cooldownTurns":1,"maxCharges":3,"chargeCost":2,"powerMultiplier":1.0,"actionPointCost":2,"code":"function executeSkill(context) { console.log('[Earthshatter] Skill executing'); console.log('[Earthshatter] sourcePiece:', sourcePiece.name, 'at', sourcePiece.x, sourcePiece.y); const targetPos = selectTarget({ type: 'grid', range: 99, filter: 'all' }); console.log('[Earthshatter] targetPos:', targetPos); if (!targetPos || targetPos.needsTargetSelection) { console.log('[Earthshatter] Needs target selection or no target'); return targetPos; } console.log('[Earthshatter] Checking piece at target position'); const pieceAtPos = battle.pieces.find(p => p.x === targetPos.x && p.y === targetPos.y && p.currentHp > 0); if (pieceAtPos) { console.log('[Earthshatter] Position occupied by:', pieceAtPos.name); return { message: '该位置已有棋子', success: false }; } console.log('[Earthshatter] Moving sourcePiece to', targetPos.x, targetPos.y); sourcePiece.x = targetPos.x; sourcePiece.y = targetPos.y; console.log('[Earthshatter] sourcePiece new position:', sourcePiece.x, sourcePiece.y); const damageValue = sourcePiece.attack * context.skill.powerMultiplier; console.log('[Earthshatter] Damage value:', damageValue); const affectedEnemies = []; for (let dx = -1; dx <= 1; dx++) { for (let dy = -1; dy <= 1; dy++) { const checkPos = { x: targetPos.x + dx, y: targetPos.y + dy }; const enemyAtPos = battle.pieces.find(p => p.x === checkPos.x && p.y === checkPos.y && p.currentHp > 0 && p.ownerPlayerId !== sourcePiece.ownerPlayerId); if (enemyAtPos) { affectedEnemies.push(enemyAtPos); } } } if (affectedEnemies.length > 0) { const result = dealDamage(sourcePiece, affectedEnemies, damageValue, 'physical', battle, context.skill.id); return { message: sourcePiece.name + '从天而降，对' + affectedEnemies.map(e => e.name).join(', ') + '造成' + result.totalDamage + '点伤害', success: true }; } return { message: sourcePiece.name + '从天而降', success: true }; }","previewCode":"function calculatePreview(piece, skillDef) { const damageValue = Math.round(piece.attack * skillDef.powerMultiplier); return { description: '选择1个空地格落下，对3x3区域内敌人造成' + damageValue + '点物理伤害。', expectedValues: { damage: damageValue, area: '3x3' } }; }"}
