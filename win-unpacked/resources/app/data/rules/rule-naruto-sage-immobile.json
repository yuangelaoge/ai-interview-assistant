{
  "id": "rule-naruto-sage-immobile",
  "name": "仙人打坐",
  "description": "仙人模式期间无法移动",
  "trigger": { "type": "beforeMove" },
  "skillCode": "var piece = context.rulePiece; if (!piece || piece.currentHp <= 0) return { success: false, message: '' }; if (!context.piece || context.piece.instanceId !== piece.instanceId) return { success: false, message: '' }; var tag = piece.statusTags && piece.statusTags.find(function(t) { return t.type === 'sage-mode'; }); if (!tag) { removeRuleById(piece.instanceId, 'rule-naruto-sage-immobile'); return { success: false, message: '' }; } return { success: true, blocked: true, message: piece.name + '正在打坐，无法移动' };"
}
