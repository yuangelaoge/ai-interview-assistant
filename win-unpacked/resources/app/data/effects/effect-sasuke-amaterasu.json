{
  "id": "effect-sasuke-amaterasu",
  "name": "天照",
  "icon": "🔥",
  "color": "#dc2626",
  "visible": false,
  "triggers": [
    {
      "on": "afterMove",
      "priority": 50,
      "filterCode": "function(ctx, battle, self) { if (!battle.extensions || !battle.extensions.amaterasuCells || battle.extensions.amaterasuCells.length === 0) return false; var movedPiece = ctx.sourcePiece; return !!(movedPiece && movedPiece.currentHp > 0); }",
      "effectCode": "function(ctx, battle, self) { if (!battle.extensions || !battle.extensions.amaterasuCells || battle.extensions.amaterasuCells.length === 0) return { success: false }; var movedPiece = ctx.sourcePiece; if (!movedPiece || movedPiece.currentHp <= 0) return { success: false }; var lastMoveAction = null; for (var i = battle.actions.length - 1; i >= 0; i--) { if (battle.actions[i].type === 'move' && battle.actions[i].payload && battle.actions[i].payload.pieceId === movedPiece.instanceId) { lastMoveAction = battle.actions[i]; break; } } if (!lastMoveAction) return { success: false }; var fromX = lastMoveAction.payload.fromX; var fromY = lastMoveAction.payload.fromY; var toX = movedPiece.x; var toY = movedPiece.y; var pathCells = []; if (fromX === toX) { var stepY = fromY < toY ? 1 : -1; for (var y = fromY + stepY; stepY > 0 ? y <= toY : y >= toY; y += stepY) pathCells.push({ x: toX, y: y }); } else if (fromY === toY) { var stepX = fromX < toX ? 1 : -1; for (var x = fromX + stepX; stepX > 0 ? x <= toX : x >= toX; x += stepX) pathCells.push({ x: x, y: toY }); } else { pathCells.push({ x: toX, y: toY }); } var amaterasuHit = 0; pathCells.forEach(function(cell) { if (battle.extensions.amaterasuCells.some(function(c) { return c.x === cell.x && c.y === cell.y; })) amaterasuHit++; }); if (amaterasuHit === 0) return { success: false }; var tag = movedPiece.statusTags && movedPiece.statusTags.find(function(t) { return t.type === 'amaterasu-burn'; }); var newStacks; if (tag) { tag.stacks = (tag.stacks || 1) + amaterasuHit; newStacks = tag.stacks; } else { addStatusEffectById(movedPiece.instanceId, { id: 'amaterasu-burn-' + movedPiece.instanceId, name: '天照灼烧', type: 'amaterasu-burn', currentDuration: -1, currentUses: -1, intensity: 2, stacks: amaterasuHit }); newStacks = amaterasuHit; } return { success: true, message: movedPiece.name + '经过天照范围，附加' + amaterasuHit + '层天照效果（当前' + newStacks + '层）' }; }"
    },
    {
      "on": "endTurn",
      "priority": 50,
      "filterCode": "function(ctx, battle, self) { return !!(battle.extensions && battle.extensions.amaterasuCells && battle.extensions.amaterasuCells.length > 0); }",
      "effectCode": "function(ctx, battle, self) { if (!battle.extensions || !battle.extensions.amaterasuCells || battle.extensions.amaterasuCells.length === 0) return { success: false }; var currentPlayerId = battle.turn.currentPlayerId; var amaterasuOwnerPlayerId = (battle.extensions && battle.extensions.amaterasuOwnerPlayerId) || null; var msgs = []; var allAlivePieces = battle.pieces.filter(function(p) { return p.currentHp > 0; }); allAlivePieces.forEach(function(p) { var isOn = battle.extensions.amaterasuCells.some(function(c) { return c.x === p.x && c.y === p.y; }); if (!isOn) return; var tag = p.statusTags && p.statusTags.find(function(t) { return t.type === 'amaterasu-burn'; }); if (tag) { tag.stacks = (tag.stacks || 1) + 1; msgs.push(p.name + '站在天照上，层数加深至' + tag.stacks + '层'); } else { addStatusEffectById(p.instanceId, { id: 'amaterasu-burn-' + p.instanceId, name: '天照灼烧', type: 'amaterasu-burn', currentDuration: -1, currentUses: -1, intensity: 2, stacks: 1 }); msgs.push(p.name + '站在天照上，获得1层天照效果'); } }); var myPieces = battle.pieces.filter(function(p) { return p.currentHp > 0 && p.ownerPlayerId === currentPlayerId; }); myPieces.forEach(function(p) { var tag = p.statusTags && p.statusTags.find(function(t) { return t.type === 'amaterasu-burn'; }); if (!tag) return; var stacks = tag.stacks || 1; var dmg = 2 * stacks; var result = dealDamage(p, p, dmg, 'true', battle, 'amaterasu-burn-tick', false, amaterasuOwnerPlayerId); var actualDmg = (result && result.damage != null) ? result.damage : 0; if (actualDmg > 0) { msgs.push(p.name + '受天照灼烧(' + stacks + '层)，失去' + actualDmg + '点生命值'); } else if (result && result.success === false && result.message && result.message !== '') { msgs.push(p.name + '天照灼烧被抵挡'); } }); if (msgs.length === 0) return { success: false }; return { success: true, message: msgs.join('；') }; }"
    }
  ]
}
