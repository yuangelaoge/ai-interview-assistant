{
  "id": "effect-rafaam-temporal-distortion",
  "name": "时空扭曲",
  "icon": "⏰",
  "color": "#a855f7",
  "visible": true,
  "triggers": [
    {
      "on": "beginTurn",
      "priority": 50,
      "filterCode": "function(ctx, battle, self) { return true; }",
      "effectCode": "function(ctx, battle, self) { var currentPlayerId = battle.turn.currentPlayerId; var contextPlayerId = ctx.playerId; if (!contextPlayerId) return { success: false }; var player = battle.players.find(function(p) { return p.playerId === contextPlayerId; }); if (!player) return { success: false }; if (!player.statusTags) player.statusTags = []; var enemyEffect = player.statusTags.find(function(t) { return t.type === 'temporal-distortion-enemy'; }); var selfEffect = player.statusTags.find(function(t) { return t.type === 'temporal-distortion-self'; }); var message = ''; var effectTriggered = false; if (selfEffect && currentPlayerId === contextPlayerId) { player.actionPoints += 1; message += '时空扭曲使自身行动点+1'; effectTriggered = true; player.statusTags = player.statusTags.filter(function(t) { return t.type !== 'temporal-distortion-self'; }); } if (enemyEffect && currentPlayerId === contextPlayerId) { if (player.actionPoints > 0) { player.actionPoints -= 1; message += (message ? '；' : '') + '时空扭曲使行动点-1'; effectTriggered = true; } player.statusTags = player.statusTags.filter(function(t) { return t.type !== 'temporal-distortion-enemy'; }); } if (!player.statusTags.some(function(t) { return t.type === 'temporal-distortion-self' || t.type === 'temporal-distortion-enemy'; })) { self.expire(); } if (effectTriggered) { return { success: true, message: message }; } return { success: false }; }"
    }
  ]
}
