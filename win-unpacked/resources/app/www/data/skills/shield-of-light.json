{
  "id": "shield-of-light",
  "name": "圣光盾",
  "description": "使7格内1个友军获得圣盾，抵挡一次伤害。",
  "keywords": ["圣盾"],
  "targetText": "7格内1个友军",
  "effectTags": ["圣盾"],
  "icon": "shield",
  "kind": "active",
  "type": "normal",
  "cooldownTurns": 5,
  "maxCharges": 0,
  "powerMultiplier": 1,
  "actionPointCost": 1,
  "code": "function executeSkill(context) { const sourcePiece = context.piece; const targetAlly = selectTarget({ type: 'piece', range: 7, filter: 'ally' }); if (!targetAlly || targetAlly.needsTargetSelection) { return targetAlly; } if (typeof addStatusEffectById === 'function') { addStatusEffectById(targetAlly.instanceId, { id: 'divine-shield', type: 'divine-shield', currentDuration: -1, currentUses: -1, intensity: 1, stacks: 1, relatedRules: ['rule-divine-shield'] }); } if (typeof addRuleById === 'function') { addRuleById(targetAlly.instanceId, 'rule-divine-shield'); } if (typeof addSkillById === 'function') { addSkillById(targetAlly.instanceId, 'divine-shield-defense'); } var selfCast = targetAlly.instanceId === sourcePiece.instanceId; if (selfCast) { var player = context.battle.players.find(function(p) { return p.playerId === sourcePiece.ownerPlayerId; }); if (player) { player.actionPoints = Math.min(player.actionPoints + 1, player.maxActionPoints); } } return { message: sourcePiece.name + '为' + targetAlly.name + '施加了圣盾' + (selfCast ? '，并回复了1点行动点' : ''), success: true }; }",
  "previewCode": "function calculatePreview(piece, skillDef) { return { description: '使7格内1个友军获得圣盾，抵挡一次伤害。', expectedValues: {} }; }",
  "range": "single"
}
